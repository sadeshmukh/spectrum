# sample .env

# GitHub OAuth Configuration
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# NextAuth config
AUTH_SECRET=
AUTH_TRUST_HOST=true

# Admin email (returned by Github)
ADMIN_EMAIL=

# -------------------------
# Cloudflare R2 Configuration
# -------------------------
# Technically, any S3 compatible provider works - 
# there might be some R2 specific quirks I had to work around, 
# but I'm not sure how much is left after editing

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=

# this might be out of date after I've updated code - I'll revisit this later

# -------------------------
# Astro DB settings
# -------------------------

# Astro docs promote Turso - free plan for now is generally good
# any libsql db does - it's a fork of sqlite, so 
# local shouldn't be terrible
# turso also offers on-device variant, currently in alpha

# Remote libSQL database URL returned by `turso db show <dbname>`
ASTRO_DB_REMOTE_URL=

# App token generated via `turso db tokens create <dbname>`
ASTRO_DB_APP_TOKEN= 